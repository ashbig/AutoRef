
Application configuration:


a. change ApplicationHostSettings file
a. build the following directories (min set)

            - trace_files_damp
            - trace_files_root
            - output
                - blast_output
                - needle_output
                - tmp_needle_output
                - tmp
b. setup scheduled jobs
    i. clean up jobs
    ii. transfer of blastable databases from FLEX server


c. install and setup cygwin for Win machine
d. rebuild all third party programs and setup them
                1. EMBOSS package
                2. primer3 (can be transferred as .exe on WIn)
                3. assembly package
                4. blast
e. set phredPhrap script according to machine settings

f. set values of program/directory passes in the following java files


file :  edu.harvard.med.hip.bec.Constants.getTemporaryFilesPath()
file:       TraceFileProcessingRunner
file:       PhredWrapper
file:       EndReadsWrapperRunner
file:       PhredPhrap
file:       PolymorphismDetector
file:       NeedleWrapper
file:       BlastWrapper
file:       FileOperations
file:       Contig
file:       DiscrepancyFinder
file:       Primer3Wrapper
file:       Constants
file:       OutstandingJobs 


3.	To reconfigure from Win to Unix 
            a.	Verify all steps in 2.
            b.	change and recompile trimming_java_script.java, put
 trimming_java_script$classes into phredPhrap directory
            
            d. change following java file that manipulate file passes

package edu.harvard.med.hip.bec.programs.phred;
class   PhredPhap
class   PhredWrapper

package edu.harvard.med.hip.bec.programs.needle;
class    NeedleWrapper

recompile trimming script and configure phrepPhap to run it



PhredPhrap setup
1. set perl scripts

addReads2Consed.perl
findSequenceMatchesForConseq.perl
phredPhrap.perl
tagRepeats.perl
transferConsensusTags.perl

2. copy vector libraries to some location and update ApplicationHostSettings

3. copy phredpar.dat to some location


Programs testing
1. assembler

perl c:\\programs_bio\\biolocal\\phredPhrap   --clonepath  /C/bio/plate_analysis/clone_samples/46691/151110 --outputfilename a


2. needle
c:\EMBOSS-2.5.1\emboss\needle.exe /c/tmp_assembly/needle1.in /c/tmp_assembly/needle1188.in -gapopen 10.0 -gapextend 0.5 -outfile c:\tmp_assembly\needle1_1188.out

      
3. primer3

4. phred compiled for windows

5. blast
